<%= form_for([:admin, @admin_message]) do |f| %>
  <% if @admin_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% @admin_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.text_field :email, class: "form-control", placeholder: "Email" %>
  </div>
  <div class="form-group">
    <%= f.text_field :title, class: "form-control", placeholder: "Title" %>
  </div>
  <%= f.text_area :body, class: "form-control", placeholder: "Body" %>
  <br />

  <div class="actions pull-right">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
